using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElderPlayer : Player
{
    // Start is called before the first frame update
    void Start()
    {
        moveSpeed = 4;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
