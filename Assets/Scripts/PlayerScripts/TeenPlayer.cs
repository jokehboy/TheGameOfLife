using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeenPlayer : Player
{
    // Start is called before the first frame update
    void Start()
    {
        moveSpeed = 14;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
