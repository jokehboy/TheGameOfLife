using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiddleAgePlayer : Player
{
    // Start is called before the first frame update
    void Start()
    {
        moveSpeed = 7;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
